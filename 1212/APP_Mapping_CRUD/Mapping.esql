
CREATE FUNCTION MappingTest (IN Id CHARACTER) RETURNS CHARACTER
BEGIN
	--DECLARE EmpId INTEGER;
	DECLARE msg INTEGER;
	SET OutputRoot.XMLNSC.Data=msg;
	--SET EmpId = CAST(Id AS CHARACTER);
	RETURN Id;
END;