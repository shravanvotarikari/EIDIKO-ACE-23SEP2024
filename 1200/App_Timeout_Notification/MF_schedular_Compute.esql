

CREATE COMPUTE MODULE MF_schedular_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
				SET OutputRoot = InputRoot;
			INSERT INTO Database.SCHEDULAR (SCHEDULAR ) VALUES (CURRENT_TIMESTAMP );
		RETURN TRUE;
	END;
END MODULE;
