

CREATE COMPUTE MODULE route_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		
		 CALL CopyEntireMessage();
		 
		RETURN TRUE;
	END;

	CREATE PROCEDURE CopyEntireMessage() BEGIN
		--SET OutputRoot = InputRoot;
		
		
		DECLARE res INTEGER;
		
		SET res = CAST (InputRoot.XMLNSC.Root.Data.OperationSet.add.a1 as INTEGER) + CAST (InputRoot.XMLNSC.Root.Data.OperationSet.add.a2 as INTEGER);
		
		SET OutputRoot.XMLNSC.result = res;
		
		
	END;
END MODULE;
