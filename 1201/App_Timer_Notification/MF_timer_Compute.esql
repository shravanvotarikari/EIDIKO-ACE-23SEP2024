

CREATE COMPUTE MODULE MF_timer_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		
		INSERT INTO Database.TIME (TIME_AND_DATE) VALUES (CURRENT_TIMESTAMP );
		
		RETURN TRUE;
	END;

	
END MODULE;
